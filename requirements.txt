# build_site.py 依赖（各子服务复用模块 + 静态数据生成）
# 说明：本机可直接复用 D:\WorkBuddy\GEX观察\.venv（依赖最全，含 pandas/numpy/openpyxl/requests）
# 若需独立环境：venv\Scripts\python.exe -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
pandas==3.0.3
numpy==2.5.0
openpyxl==3.1.5
requests==2.34.2
chinese_calendar==1.11.0
